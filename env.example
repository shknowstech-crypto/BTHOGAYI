# Supabase Configuration
# Your actual Supabase project credentials
# Database Configuration
# Connect to Supabase via connection pooling (recommended for production)
DATABASE_URL=postgresql://your-user:your-password@your-host:6543/postgres?pgbouncer=true

# Direct connection to the database (used for migrations)
DIRECT_URL=postgresql://your-user:your-password@your-host:5432/postgres

# Frontend Environment Variables
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Recommendation Engine API URL (Render backend)
NEXT_PUBLIC_RECOMMENDATION_API_URL=https://your-recommendation-engine.onrender.com

# Google OAuth Configuration
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id

# ======== RECOMMENDATION ENGINE CONFIG (.env for recommendation-engine folder) ========

# API Security Configuration
# Secret key for JWT token signing and verification (generate with: openssl rand -hex 32)
API_SECRET_KEY=your-super-secure-secret-key-change-this-in-production

# API key for frontend authentication (generate with: openssl rand -hex 32)
API_KEY=your-frontend-api-key-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: https://your-app.vercel.app,https://your-staging.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app

# Trusted hosts (comma-separated)
# Include your Render domain
TRUSTED_HOSTS=localhost,127.0.0.1,your-app-name.onrender.com

# Environment Configuration
# Set to 'development' for development, 'production' for production  
ENVIRONMENT=production

# Server Configuration
# Port for the FastAPI server (default: 8000)
PORT=8000

# Supabase Integration (for JWT verification)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Redis Configuration (for rate limiting and caching)
# Use Render Redis add-on URL in production
REDIS_URL=redis://red-xxx:6379

# Algorithm Configuration
# JWT algorithm (default: HS256)
ALGORITHM=HS256

# Logging Level
# Set logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Google OAuth (if you want to enable Google sign-in)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret 