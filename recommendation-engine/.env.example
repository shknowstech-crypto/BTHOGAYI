# BITSPARK Backend Environment Variables - Production Ready
# Copy these to your Render service environment variables

# Database Configuration (Required)
DATABASE_URL=postgresql://user:password@host:port/database
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Security Configuration (Required)
API_SECRET_KEY=your-super-secret-key-here-min-32-chars
API_KEY=your-api-key-for-frontend-access

# Application Configuration
ENVIRONMENT=production
LOG_LEVEL=INFO
PORT=10000

# CORS Configuration (Required)
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com
ALLOWED_HOSTS=your-backend.onrender.com,localhost

# Rate Limiting (Optional)
RATE_LIMIT_PER_MINUTE=30
REDIS_URL=memory://

# Recommendation Engine (Optional)
RECOMMENDATION_CACHE_TTL=300
MAX_RECOMMENDATIONS=50
MIN_COMPATIBILITY_SCORE=0.5

# Monitoring & Analytics (Optional)
SENTRY_DSN=https://your-sentry-dsn
ANALYTICS_ENABLED=true
=======
# PostgreSQL connection string for the recommendation engine database
DATABASE_URL=postgresql://username:password@localhost:5432/bitspark_db

# API Security Configuration
# Secret key for JWT token signing and verification
API_SECRET_KEY=your-super-secure-secret-key-change-this-in-production

# API key for frontend authentication (should be a secure random string)
API_KEY=your-frontend-api-key-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# Environment Configuration
# Set to 'development' for development, 'production' for production
ENVIRONMENT=development

# Server Configuration
# Port for the FastAPI server (default: 8000)
PORT=8000

# Optional: Redis Configuration
# Redis connection URL for caching (if using Redis)
# REDIS_URL=redis://localhost:6379

# Optional: Algorithm Configuration
# JWT algorithm (default: HS256)
# ALGORITHM=HS256

# Optional: Logging Level
# Set logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Optional: Supabase Integration (if needed)
# VITE_SUPABASE_URL=https://your-supabase-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Google OAuth (if needed)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Email Service (if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@yourdomain.com
>>>>>>> Stashed changes
